# Supabase

SQL migrations in `migrations/` define the production database schema. Match data, participant accounts and predictions belong in Supabase, so redeploying the website does not erase them.
